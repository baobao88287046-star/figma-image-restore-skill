interface:
  display_name: "Figma Image Restore"
  short_description: "Restore UI screenshots into Figma layers"
  default_prompt: "Use $figma-image-restore to restore this UI screenshot into an editable Figma Web reconstruction."
policy:
  allow_implicit_invocation: true
